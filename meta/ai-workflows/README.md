# AI Workflow Notes

This folder contains project-level AI agent instructions, local skills, and subagent descriptions used while developing the repository.

These files are not part of the runtime product. They are kept under `meta/` so the public repository root stays focused on the product demo, source code, documentation, and verification workflow.
